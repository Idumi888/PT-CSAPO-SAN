{% extends 'base.html.twig' %}

{% block title %}{{parent()}}Liste des élèves{% endblock %}
{% block content %}


<div class="container-fluid">
            <div class="row justify-content-center">
                <h1 class="text-center  p-4">Liste des élèves</h1>
            </div>
            <div class="row justify-content-center">
                <div class="col-8 p-4 m-0 ">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th scope="col">
                                        Nom 
                                    </th>
                                    <th scope="col">
                                        Prénom
                                    </th>
                                    <th scope="col">
                                        Photo
                                    </th>
                                    
                                </tr>
                            </thead>
                            <tbody>
                                {% for eleve in eleve %}
                                    <tr class="{{ cycle(['table-primary', 'table-secondary'], loop.index0) }}">
                                   
                                        <td>
                                            {{eleve.getNom |capitalize }}
                                        </td>
                                        <td>
                                            {{eleve.getPrenom |capitalize }}
                                        </td>
                                        <td>
                                            {{eleve.getPhoto |capitalize }}
                                        </td>
                                        <td>
                                        {% if is_granted('ROLE_ADMIN') %}
    

                                            <a href=""><span class="material-icons">
                                                create
                                            </span></a>
                                        </td>
                                        <td>
                                            <a href="{{path('liste_epreuves',{'supp':epreuve.id})}}" class="text-white">
											<span class="material-icons" title="Supprimer le vocabulaire">
												delete
											</span>
										</a>
                                        </td>
                                    {% endif %}
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
{% if is_granted('ROLE_ADMIN') %}
            {{form(form)}}

{% endif %}
        </div>


{% endblock %}